<h1>Campus Anywhere is one step away.</h1>
<div class="row">  
  <div class="col-md-6 col-md-offset-3">
    <p class="flash"><%= alert %></p>
    <%= form_with(url: sign_path)  do |form| %>
    <%= render 'users/form', f: form %>
    <%= form.submit :"Create Account", class: "form-button" %>
    <% end %>
  </div>
</div>